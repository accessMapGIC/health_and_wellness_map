import * as actionTypes from '../actions';

const initialState = {
    
}

const dbReducer = (state = initialState, action ) => {
    
    
    switch ( action.type ){
        
        default: 
            return state;
    }
}




export default dbReducer;